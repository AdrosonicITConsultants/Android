<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:card_view="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="event"
            type="com.adrosonic.craftexchange.ui.interfaces.CategoryProductClick"/>

        <variable
            name="categoryDetails"
            type="com.adrosonic.craftexchange.repository.data.response.buyer.viewProducts.Product"/>

    </data>


    <androidx.cardview.widget.CardView
        android:id="@+id/category_prod_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_regular"
        android:elevation="@dimen/margin_small"
        android:onClick="@{()->event.onItemClick(categoryDetails)}">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:weightSum="1"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/prod_img"
                android:src="@drawable/demo_img"
                android:layout_width="match_parent"
                android:layout_height="130dp"
                android:scaleType="centerCrop"/>

            <TextView
                android:id="@+id/prod_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black_text"
                android:fontFamily="@font/playfair_regular"
                android:layout_marginTop="@dimen/margin_small"
                android:layout_marginBottom="@dimen/margin_small"
                android:layout_gravity="center"
                android:textAlignment="center"
                android:textSize="16sp" />

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>